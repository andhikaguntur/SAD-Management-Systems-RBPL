type NormalizeOptions = {
    stripPlusAliases?: boolean;
};
/**
 * Normalize an email address according to provider-specific rules.
 */
export declare function normalizeEmail(email: string, options?: NormalizeOptions): string | null;
export {};
//# sourceMappingURL=account.utils.d.ts.map